{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build HM",
            "type": "shell",
            "command": "cd ${workspaceFolder}/build/linux/; and make -f makefile;",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}